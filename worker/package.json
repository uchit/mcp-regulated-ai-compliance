{
  "name": "mcp-regulated-ai-compliance-worker",
  "version": "0.2.1",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker deployment of mcp-regulated-ai-compliance — hosted Streamable HTTP endpoint for Smithery, ChatGPT MCP directory, browser clients, and any hosted MCP consumer.",
  "scripts": {
    "embed-data": "node scripts/embed-data.mjs",
    "build": "npm run embed-data",
    "dev": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "tail": "wrangler tail",
    "smoke": "node scripts/smoke.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0",
    "zod-to-json-schema": "^3.23.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.99.0"
  }
}
